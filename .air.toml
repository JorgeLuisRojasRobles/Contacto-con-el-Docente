root = "."
tmp_dir = "tmp"

[build]
  # Apuntamos al main.go dentro de cmd/api
  cmd = "go build -o ./tmp/main ./cmd/api/main.go"
  bin = "./tmp/main"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["assets", "tmp", "vendor", "bin"]